#pragma once

#include <string>

namespace dungeon::baker {

// Renders the landing-page background (title_bg.png) into <texturesDir>.
bool BakeTitleImage(const std::string& texturesDir);

} // namespace dungeon::baker
