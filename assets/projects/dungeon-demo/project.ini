; Dungeon Demo — project manifest.

name = Dungeon Demo
; showcase leads, so a new game opens on the composed level; the two gallery
; floors (start / level2) stay available for auditing surfaces.
levels = showcase start level2
default_sconce = sconce
default_brazier = brazier
